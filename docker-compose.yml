services:
  botc-ai:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DATABASE_URL: sqlite:////app/data/botc_ai.sqlite3
    volumes:
      - botc-data:/app/data

volumes:
  botc-data:
